name: gyawun_metadata_sdk
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.10.7

# Add regular dependencies here.
dependencies:
  eval_annotation: ^0.8.1
  dart_eval: ^0.8.4
dev_dependencies:
  build_runner: ^2.15.1
  build_version: ^2.1.3
  lints: ^6.0.0
  test: ^1.25.6
